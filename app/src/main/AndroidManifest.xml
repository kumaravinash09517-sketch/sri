<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.blurr.voice.v2"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Minimal required permissions left in place -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:label="Sarkar AI Receptionist"
        android:icon="@android:drawable/sym_def_app_icon">

        <!-- Accessibility service declaration: keep meta-data reference only if the xml exists. -->
        <service
            android:name=".AgentService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <!-- If res/xml/accessibility_service_config.xml is missing, this meta-data is harmless but
                 you should add a valid config if you intend to enable the service later. -->
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Launcher activity (safe, minimal UI) -->
        <activity
            android:name=".PermissionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
